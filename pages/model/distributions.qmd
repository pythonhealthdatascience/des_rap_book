---
title: ❌ Randomness
---

:::{.guidelines}
* Heather et al. 2025: Control randomness.
:::

<!--TODO:
steps we are going through...

1 knowing what want to model
2 input modelling
3 importing input parametesr
4 understanding how will sample from distributions for each
5 start basic: generate arrivals
    e.g. for stroke models, generated patient arrivals for the 2 units + all patients types as first step
6 build from there - further processing

for r, when constructing, need to add to importfrom, then run devtoolsdocument, then loadall, then try it out
otherwise it won't find the stuff you importfrom if dont document first as needs to add to namespace

rexp requires 1 / inter-arrival time for rate as rexp requires rate and rate is the inverse of mean inter-arrival time

consider: often logging at this early stage... python print().... simmer verbose=True...
-->